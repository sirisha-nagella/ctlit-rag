.venv
.git
.gitignore
__pycache__
*.pyc
.DS_Store

# not needed in the serving image
archive/
tests/
screenshots/
README.md

# dead vector-store artifacts (Chroma is the live store, not FAISS)
vector_store.faiss
embeddings.npy
chunks.json

# raw data + build-time modules (only needed to REBUILD the store, not to serve it)
data
data_sources
chunking.py
